import { useState } from 'react';
import Canvas from '../UI/Canvas';

function Cfrom(props) {

    return (
        <div style={{ 'padding': '20px' }}>
            <Canvas />
            Clock
        </div>
    );
}

export default Cfrom;
